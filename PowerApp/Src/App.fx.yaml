App As appinfo:
    BackEnabled: =false
    OnStart: =Set(varUniformView,false);Set(varAnnouncementVisible,true);Refresh('Announcements List');Refresh('Student Hub - Document List')
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

